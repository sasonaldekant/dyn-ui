import { DynContainer } from '../DynContainer'; // adjust path as necessary based on actual file location
describe('DynContainer', () => {
  it('exports a React component', () => {
    expect(typeof DynContainer).toBe('function');
  });
});
