import type { Meta, StoryObj } from '@storybook/react-vite';
import { DynBadge } from './DynBadge';
import { DynBadgeProps } from '../../types/badge.types';
import { IconDictionaryProvider } from '../../providers/IconDictionaryProvider';

const meta: Meta<typeof DynBadge> = {
  title: 'Display Components/DynBadge',
  component: DynBadge,
  decorators: [
    (Story) => (
      <IconDictionaryProvider>
        <div style={{ padding: '1rem' }}>
          <Story />
        </div>
      </IconDictionaryProvider>
    ),
  ],
  parameters: {
    layout: 'centered',
  },
  tags: ['autodocs'],
  argTypes: {
    value: { control: 'number' },
    status: {
      control: 'select',
      options: ['positive', 'negative', 'warning', 'disabled'],
    },
    color: { control: 'text' },
    size: {
      control: 'select',
      options: ['small', 'medium', 'large'],
    },
    icon: { control: 'text' },
    showBorder: { control: 'boolean' },
  },
};

export default meta;
type Story = StoryObj<typeof meta>;

export const Playground: Story = {
  args: {
    value: 5,
    status: 'positive',
    size: 'medium',
    icon: undefined,
    color: undefined,
    showBorder: false,
  } as DynBadgeProps,
};

export const StatusColors: Story = {
  render: () => (
    <div style={{ display: 'flex', gap: '0.5rem', flexWrap: 'wrap' }}>
      <DynBadge value={8} status="positive" />
      <DynBadge value={2} status="negative" />
      <DynBadge value={3} status="warning" />
      <DynBadge value={0} status="disabled" />
    </div>
  ),
};

export const WithIcons: Story = {
  render: () => (
    <div style={{ display: 'flex', gap: '0.5rem', flexWrap: 'wrap' }}>
      <DynBadge value={5} status="positive" icon="ok" />
      <DynBadge value={2} status="warning" icon="warning" />
      <DynBadge value={3} status="negative" icon="close" />
    </div>
  ),
};

export const BorderVariant: Story = {
  render: () => (
    <div style={{ display: 'flex', gap: '0.5rem' }}>
      <DynBadge value={10} status="positive" showBorder />
      <DynBadge value={5} status="warning" showBorder />
      <DynBadge value={0} status="negative" showBorder icon="close" />
    </div>
  ),
};

export const Sizes: Story = {
  render: () => (
    <div style={{ display: 'flex', gap: '0.5rem', alignItems: 'center' }}>
      <DynBadge value={1} size="small" />
      <DynBadge value={2} size="medium" />
      <DynBadge value={3} size="large" />
    </div>
  ),
};