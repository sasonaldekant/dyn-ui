/**
 * TypeScript interfaces for DynBadge component
 * Re-exports from centralized badge types for consistency
 */

export type {
  DynBadgeProps,
  BadgeStatus,
  BadgeSize,
  BadgeIcon
} from '../../types/badge.types';

export { DYN_COLOR_PALETTE } from '../../types/badge.types';